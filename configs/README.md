# Configs

TOML configs for the 4 reported runs. See `../SETUP.md` to run these.

- `all300_solver_only_gpt55.toml` / `all300_pocharness_gpt55.toml` — A-only
  vs. A+B+C, GPT-5.5. Headline scaffold result.
- `all300_solver_only_gpt54mini.toml` / `all300_pocharness_gpt54mini.toml` —
  same, GPT-5.4-mini (A-only follows SEC-bench's original poc-desc prompt).
