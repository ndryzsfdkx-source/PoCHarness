# pocharness

Orchestration for the reported runs: `run_secbench_poc.py` (generate +
evaluate) and `analyze_run.py` (grader-count reports). Pure stdlib — wraps
the vendored smolagents CLI and the external SEC-bench evaluator rather than
reimplementing either. See `../../SETUP.md`.
